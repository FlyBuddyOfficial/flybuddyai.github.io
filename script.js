// Show Chat with Animation
function scrollToChat() {
    let chatBox = document.getElementById("chat-container");
    chatBox.style.display = "block";
    chatBox.style.opacity = 0;
    chatBox.style.transition = "opacity 0.
